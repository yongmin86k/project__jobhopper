const styles = theme => ({
  filterBtn: {
    boxShadow: "none",
    marginRight: theme.spacing(2),
    "&:hover": {
      boxShadow: "none",
      backgroundColor: "rgba(0, 0, 0, 0.08)"
    }
  }
});

export default styles;
