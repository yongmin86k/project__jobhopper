import React from "react";
import { JobCard } from "/imports/ui/components";

const SingleJob = ({ previewValue }) => {
  return <JobCard previewValue={previewValue} />;
};

export default SingleJob;
